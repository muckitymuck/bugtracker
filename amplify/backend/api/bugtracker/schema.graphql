type Bug @model {
  bugID: ID!
  version: Int!
  description: String
  Priority: Int
  Title: String
}

type Mutation {
  bugtrackerTrigger1(id: Int, version: Int, description: String, Priority: Int, Title: String): String @function(name: "bugtrackerTrigger60db96c9-dev-${env}")
}